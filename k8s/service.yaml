apiVersion: v1
kind: Service
metadata:
  name: github-pullrequest-ms
  labels:
    app: github-pullrequest-ms
spec:
  type: ClusterIP
  selector:
    app: github-pullrequest-ms
  ports:
    - name: http
      port: 80
      targetPort: 8080
